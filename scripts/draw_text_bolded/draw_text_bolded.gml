/// @param x
/// @param y
/// @param text
function draw_text_bolded() {

	var x1 = argument[0];
	var y1 = argument[1];
	var text = argument[2];

	draw_text(x1, y1, text)
	draw_text(x1+1, y1, text)


}
